import { writable } from 'svelte/store';

export const datSwitch = writable([0]);
export const datStay = writable([0]);

export const datSwitchV = writable([0,0]);
export const datStayV = writable([0,0]);
